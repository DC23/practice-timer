#include "countdownoptions.h"

CountdownOptions::CountdownOptions() :
    presetOne(0,1,0),
    presetTwo(0,5,0),
    presetThree(0,10,0),
    presetFour(0,25,0),
    playChime(true),
    showMessageBox(true),
    loop(false)
{
}
